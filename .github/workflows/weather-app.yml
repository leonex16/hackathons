name: Weather App Workflow

on:
  push:
    branches:
      - weather-app

jobs:
  avoid-actions-duplicates:
    runs-on: ubuntu-20.04
    steps:
      - name: Cancel previous actions
        uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ github.token }}

  lint-code:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18.0.0
      - uses: pnpm/action-setup@v2.2.2
        with:
          run_install: false
          version: 6.0.2
      - name: Install dependencies
        run: pnpm i
      - name: Run lint script
        run: pnpm run lint:weather

  testing-e2e-code:
    needs: 
      - lint-code
    runs-on: ubuntu-20.04
    environment: WEATHER_APP_TEST
    env:
      VITE_API_URL: ${{ secrets.VITE_API_URL }}
      VITE_RAPIDAPI_HOST: ${{ secrets.VITE_RAPIDAPI_HOST }}
      VITE_RAPIDAPI_KEY: ${{ secrets.VITE_RAPIDAPI_KEY }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18.0.0
      - uses: pnpm/action-setup@v2.2.2
        with:
          run_install: false
          version: 6.0.2
      - name: Run tests
        run: npx playwright install && pnpm run test:weather
      
